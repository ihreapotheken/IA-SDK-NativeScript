<Page 
  xmlns="http://schemas.nativescript.org/tns.xsd" 
  navigatingTo="navigatingTo" 
  backgroundSpanUnderStatusBar="false" >
    <Page.actionBar>
        <ActionBar title="ia.de AppSDK Demo" icon="">
        </ActionBar>
    </Page.actionBar>
    
    <StackLayout>
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
        <Button 
            text="Launch Dermanostic Experience" 
            tap="{{ launchDermanosticExperience }}"
            style="margin-bottom:16" />
        <Button 
            text="Logout" 
            tap="{{ logout }}"
            style="margin-bottom:16" />
    </StackLayout>
</Page>